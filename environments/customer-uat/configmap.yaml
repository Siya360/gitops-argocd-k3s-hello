apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-config
data:
  ENV_NAME: "customer-uat"
  ENV_ROLE: "external-customer-uat-simulation"
  GREETING_MESSAGE: "Hello from customer UAT simulation. I need controlled access to env-d."
  DEPENDENCY_URL: "http://hello-app.env-d.svc.cluster.local"
